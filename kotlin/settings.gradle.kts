plugins {
    // Lets the Kotlin toolchain auto-provision a JDK 21 when none is installed.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.9.0"
}

rootProject.name = "a11-kotlin"
